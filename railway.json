{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {},
  "deploy": {
    "startCommand": "python -m flask db upgrade && gunicorn app:app --bind 0.0.0.0:$PORT --timeout 60 --workers 2",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
